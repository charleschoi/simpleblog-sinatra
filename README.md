# Simple Blog - sinatra

1. Install

  ```
  bundle install
  ```

  ```
  rake db:migrate
  ```

  ```
  gem install tux # console
  gem install shotgun # autoreload server
  ```

3. Run

  ```
  shotgun
  # == Shotgun/WEBrick on http://127.0.0.1:9393/
  ```
